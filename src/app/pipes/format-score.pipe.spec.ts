import { FormatScorePipe } from './format-score.pipe';

describe('FormatScorePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatScorePipe();
    expect(pipe).toBeTruthy();
  });
});
